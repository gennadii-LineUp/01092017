export class Select2optionClass {
  id: string;
  text: string;
  // disabled?: boolean;
  // children?: Array<any>;
  // additional?: any;

  constructor( id: string,
               text: string) {
               // disabled: boolean,
               // children: Array<any>,
               // additional: any) {

    this.id = id;
    this.text = text;
    // this.disabled = disabled;
    // this.children = children;
    // this.additional = additional;
  }
}
